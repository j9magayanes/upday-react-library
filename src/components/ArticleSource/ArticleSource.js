import React from 'react';
import './articleSource.css';
import  'upday-web-components/components/articleSource'

export const ArticleSource = ({ className, text }) => {

  return (
   <article-source class={className} text={text}></article-source>
  );
};